# 复制为 .env.secrets（已在 .gitignore，仅 ECS / 生产 compose 使用，勿提交 Git）
# Copy to .env.secrets on the server — one-time setup, survives git pull & CD redeploys.
#
# Spring Boot 环境变量映射：JWT_SECRET → jwt.secret
# MySQL 密码由 docker-compose.yml 的 SPRING_DATASOURCE_PASSWORD 注入，无需写在此文件。

JWT_SECRET=replace-with-random-string-at-least-32-characters-long
