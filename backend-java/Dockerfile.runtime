# CI/CD 专用：在 Runner 上 mvn package 后，只打运行镜像（避免多阶段 Maven 在部分环境不稳定）
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app

COPY target/backend-java-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
